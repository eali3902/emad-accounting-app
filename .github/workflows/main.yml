name: Build Android App

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Create App
      run: |
        echo "بناء تطبيق أندرويد..." > build.log
        echo "✅ تم!" >> build.log
        
    - name: Upload Result
      uses: actions/upload-artifact@v3
      with:
        name: app-output
        path: build.log
